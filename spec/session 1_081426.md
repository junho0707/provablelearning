1. Get a database running. Either:
- Local: supabase start then supabase db reset (applies all migrations in supabase/migrations/ in order + seed.sql) — needs Docker, which is why I couldn't do this in my sandbox.

- Or a real Supabase project: supabase link --project-ref <ref> then supabase db push, then run seed.sql via the dashboard SQL editor.
2. Fill in .env.local from .env.local.example — at minimum Supabase URL/anon/service-role keys to run at all; add STRIPE_*, GOOGLE_*, RESEND_*, CRON_SECRET as you get to those sections of VERIFY.md.

3. Run it:
   npm install
   npm test          # 181 passing, no live deps needed
   npm run build      # should be clean
   npm run dev         # http://localhost:3000

4. Bootstrap an admin. Sign in once via /login, then in the Supabase SQL editor:
   update accounts set is_admin = true where email = 'you@example.com';
   (There's no UI for this — noted in VERIFY.md §4.)

5. Work through VERIFY.md top to bottom — §0 (env setup) → §1 (M3) → §2 (M4) → §3 (M5) → §4 (M6) → §5 (M7 launch). Each section names which migrations it needs if you're applying them incrementally rather than all at once.

One honest caveat: SETUP.md at the repo root is stale — it still describes only M1 and points at TASK-CONTENT-002 as the next task. Want me to update it to match where the project actually is now, or are you good following VERIFY.md directly?
